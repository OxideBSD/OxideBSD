[package]
name = "std-process-fs-oxidebsd"
version = "0.1.0"
edition = "2024"
publish = false

# Deliberately empty -- opts this crate OUT of the root repo's `[workspace]`, same reasoning as
# userland-std/std-hello/Cargo.toml.
[workspace]
